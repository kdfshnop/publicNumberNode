<!DOCTYPE html>
<html lang="zh-cn">
    <head>
        <meta charset="utf-8">        
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no, viewport-fit=contain">
        <title><%= head.meta.title %></title>
        <meta name="keywords" content="<%= head.meta.keywords %>">
        <meta name="description" content="<%= head.meta.description %>">
        <meta name = "format-detection" content = "telephone=no">                       
        <link rel="stylesheet" href="<%= app.appStaticPrefix %>/css/app.min.css?v=<%= app.version %>">
        <link rel="stylesheet" href="<%= app.appStaticPrefix %>/css/<%= userAgent.ios ? 'ios' : 'android' %>.min.css?v=<%= app.version %>">        
        <%  page.extraStylesheets && page.extraStylesheets.forEach(function(sheet){ %>
        <link rel="stylesheet" href="<%= sheet %>?v=<%= app.version %>">
        <% }) ; %>      
        <% if(page.matchStylesheetPath) { %><link rel="stylesheet" href="<%= app.appStaticPrefix %>/css/<%= page.matchStylesheetPath %>.min.css?v=<%= app.version %>"><% } %>        
    </head>
    <body <% if(app.appType === "mobile") { %>ontouchstart=""<% } %>>    
  